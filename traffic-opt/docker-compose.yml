version: '3.8'
services:
  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:latest
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  # Define your other services here, e.g. api, dashboard, ingest, detector, metrics, optimizer, sumo, db
  # Example:
  # api:
  #   build: ./apps/api
  #   ports:
  #     - "8000:8000"
  # dashboard:
  #   build: ./apps/dashboard
  #   ports:
  #     - "8501:8501"
  # Add other services as needed



networks:
  default:
    driver: bridge
  # traffic-net:
  #   driver: bridge
